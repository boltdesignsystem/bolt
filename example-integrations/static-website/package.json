{
  "name": "static-website",
  "version": "1.0.0",
  "description": "Simple webserver",
  "main": "index.js",
  "scripts": {
    "preinstall": "cd .bolt && yarn",
    "postinstall": "cd .bolt && yarn run build",
    "start": "serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Szałapski",
  "license": "ISC",
  "dependencies": {
    "serve": "^11.0.0"
  }
}
