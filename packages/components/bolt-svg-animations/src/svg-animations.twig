

{% if dashSize %}
  {% set attributes = attributes.setAttribute("dashSize", dashSize) %}
{% endif %}

{% if speed %}
  {% set attributes = attributes.setAttribute("speed", speed) %}
{% endif %}

<svg-animations {{ attributes }}></svg-animations>