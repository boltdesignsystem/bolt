{% set schema = bolt.data.components['@bolt-components-dialogue'].schema %}
{% if enable_json_schema_validation %}
  {{ validate_data_schema(schema, _self)|raw }}
{% endif %}

{# Variables #}
{% set this = init(schema) %}
{% set attributes = create_attribute(attributes|default({})) %}

{# Array of classes based on the defined + default props #}
{% set _outer_classes = [
  'c-bolt-dialogue',
] %}

{# Array of classes based on the defined + default props #}
{% set _inner_classes = [
  'c-bolt-dialogue__container',
  'c-bolt-dialogue__container--width-' ~ this.data.width.value,
  'c-bolt-dialogue__container--spacing-' ~ this.data.spacing.value,
  't-bolt-' ~ this.data.theme.value
] %}

<script>
  // console.log({{ this.data|json_encode() }})
</script>

{% set _icon_close %}
  {% include '@bolt-components-icon/icon.twig' with {
    name: 'close',
  } only %}
{% endset %}

{% set _close_button %}
  {% include '@bolt-elements-button/button.twig' with {
    content: 'Close',
    icon_only: _icon_close,
    hierarchy: 'secondary',
    size: 'small',
    border_radius: 'full',
    attributes: {
      type: 'button',
      class: 'c-bolt-dialogue__close',
      'data-dialogue-close': true,
      'aria-label': 'Close dialogue',
    }
  } only %}
{% endset %}

<div {{ attributes.addClass(_outer_classes) }} aria-hidden="true">
  <div class="dialogue__overlay" tabindex="-1" data-dialogue-close>
    <div class="{{ _inner_classes|join(' ') }}" role="dialog" aria-dialogue="true" aria-labelledby="{{ _uuid }}-title">
      {{ _close_button }}
      <div class="c-bolt-dialogue__content" id="{{ _uuid }}-content">
        {{content}}
      </div>
    </div>
  </div>
</div>
