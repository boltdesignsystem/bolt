{#
/**
 * @file
 * A wrapper for multiple radio input elements.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The rendered checkboxes.
 *
 * @see Drupal's radios.html.twig
 * @see Bolt's form-checkboxes.twig
 */
#}

{% extends "@bolt/form-checkboxes.twig" %}

{# @todo Validation function cannot be called in an extend of another twig template. Suggested that `form-radios.schema.yml` is deleted and removed from form package.json#}
{#{% if enable_json_schema_validation %}#}
  {#{{ validate_data_schema(bolt.data.components['@bolt-components-form'].schema.properties['form-fieldset'], _self) | raw }}#}
{#{% endif %}#}
