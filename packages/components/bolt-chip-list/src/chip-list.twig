{#
  If `noWC`, load alternate template which does return a Web Component.
  There is intentional duplication of code between these two templates,
  as we may deprecate the WC-version in the future.
#}
{% if noWC %}
  {% include "@bolt-components-chip-list/_chip-list-replacement.twig" %}
{% else %}
  {% include "@bolt-components-chip-list/_chip-list-deprecated.twig" %}
{% endif %}
