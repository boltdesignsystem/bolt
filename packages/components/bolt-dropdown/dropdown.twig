{% set collapse = collapse | default(false) %}
{% set content = content | default(block('dropdown_content')) %}

<bolt-dropdown
  {{ collapse ? "collapse" : "" }}
  {{ center ? "center" : "" }}
  {% if title %}title='{{ title }}' {% endif %}
  {% if children %}children='{{ children|json_encode() }}' {% endif %}
  >
  {% block dropdown_content %}
    {% if content %}
      {% include "@bolt-components-dropdown/_dropdown-item.twig" %}
    {% endif %}
  {% endblock %}
</bolt-dropdown>

{% if enable_json_schema_validation %}
  {{ validate_data_schema('@bolt-components-dropdown/dropdown.schema.yml', _self) | raw }}
{% endif %}