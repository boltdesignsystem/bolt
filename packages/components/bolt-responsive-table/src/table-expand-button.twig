{% set schema = bolt.data.components['@bolt-components-responsive-table'].schema %}
{% if enable_json_schema_validation %}
  {{ validate_data_schema(schema, _self)|raw }}
{% endif %}

{# Variables #}
{% set this = init(schema) %}
{% set expand_button_attributes = create_attribute(attributes|default({})) %}
{% set _icon_chevron_down %}
  {% include '@bolt-elements-icon/icon.twig' with {
    name: 'chevron-down',
  } only %}
{% endset %}

{% include '@bolt-elements-text-link/text-link.twig' with {
  content: _icon_chevron_down ~ '<span class="u-bolt-visuallyhidden">' ~ content ~ '</span>',
  attributes: {
    type: 'button',
    'aria-expanded': 'false',
    class: [
      'c-bolt-responsive-table__expand-button',
      'js-bolt-toggle-button',
    ],
  }
} only %}
