{# Create variables for blocks that can be overrided using old card schema. #}
{% set _body_block = block('card_body') %}
{% set _footer_block = block('card_footer') %}

{% if (contentItems) or (_body_block is not empty) or (_footer_block is not empty) %}
  {% include "@bolt-components-card/_old-card-adapter.twig" %}
{% else %}
  {#
  @TODO In the next major version of Bolt, remove the adaptors and replace the contents of this file (card.twig) with the contents of _new-card.twig
  #}
  {% include "@bolt-components-card/_new-card.twig" %}
{% endif %}

