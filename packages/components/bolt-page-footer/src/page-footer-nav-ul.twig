{# Twig usage 

{% include 'bolt-components-page-footer/page-footer-nav-ul.twig' with {
  headline: {
    text: 'Company',
    tag: 'h3',
  },
  content: content, // page-footer-nav-li goes here
  category: category, // [social, language, legal]
} only %}

 #}

{% set schema = bolt.data.components['@bolt-components-page-footer'].schema['page-footer-nav-ul'] %}
{% if enable_json_schema_validation %}
  {{ validate_data_schema(schema, _self)|raw }}
{% endif %}

{# Variables #}
{% set this = init(schema) %}
{% set attributes = create_attribute(attributes|default({})) %}
{% set headline_attributes = create_attribute(headline.attributes|default({})) %}
{% set icon_chevron_down %}
  {% include '@bolt-components-icon/icon.twig' with {
    name: 'chevron-down',
  } only %}
{% endset %}

{% set classes = [
  'c-page-footer__nav-item',
  this.data.category.value ? 'c-page-footer__nav-item--' ~ this.data.category.value,
] %}

{% set headline_classes = [
  'c-page-footer__nav-headline',
  this.data.headline.value ? this.data.headline.value : '',
] %}

<div {{ attributes.addClass(classes) }}>
  <{{ headline.tag ? headline.tag : 'h3' }} {{ headline_attributes.addClass(headline_classes) }}>{{ headline.text }}</{{ headline.tag ? headline.tag : 'h3' }}>
  <button class="c-page-footer__nav-headline c-page-footer__nav-headline--trigger js-bolt-page-footer-toggle-trigger" type="button" aria-expanded="false">
    {{ headline.text }}
    <span class="c-page-footer__nav-headline--trigger__icon" aria-hidden="true">{{ icon_chevron_down }}</span>
  </button>
  <ul class="c-page-footer__nav-list">
    {{ content }}
  </ul>
</div>
