$schema: http://json-schema.org/draft-04/schema#
title: Background shapes
type: object
properties:
  shapeGroup:
    type: string
    description: Default is `A`. The shapeGroup to use to build the shapes layout.
    enum:
      - A
      - B