## Demo: Render Custom Footer UI {id=render-custom-footer-ui}

In these example, we configure Typeahead to use the new `footerTemplate` web component property to render out custom UI below the search results.