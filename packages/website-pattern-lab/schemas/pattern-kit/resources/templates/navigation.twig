<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#patternkit-main-nav" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">{{ title }}</a>
    </div>
    <div class="collapse navbar-collapse" id="patternkit-main-nav">
        <ul class="nav navbar-nav">
            {% for key, category in categories %}
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    {{ category.title ?: key }}s <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                    {% for item in category.items %}
                    <li {{ item.active ? 'class="active"' }}>
                        <a href=" {{ path( 'schema', { pattern : item.path } ) }} ">{{ item.title }}</a>
                    </li>
                    {% endfor %}
                </ul>
            </li>
            {% endfor %}
        </ul>
        {% if sg_active %}
        <ul class="nav navbar-nav navbar-right">
            <li><a href="/sg/">Styleguide</a></li>
        </ul>
        {% endif %}
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
