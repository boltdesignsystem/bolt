import { BoltElement } from './src/BoltElement';
import { BoltActionElement } from './src/BoltActionElement';
import { Slotify } from './src/Slotify';
import {
  hasNativeShadowDomSupport, // deprecated, use supportsShadowDom instead
  shouldUseShadowDom,
  supportsShadowDom,
  findParentTag,
} from './src/lib/utils';

export { html, customElement, unsafeCSS } from 'lit-element';

export {
  BoltElement,
  BoltActionElement,
  Slotify,
  hasNativeShadowDomSupport, // deprecated, use supportsShadowDom instead
  shouldUseShadowDom,
  supportsShadowDom,
  findParentTag,
};
