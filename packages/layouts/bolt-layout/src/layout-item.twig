{% set item_attributes = create_attribute(attributes|default({})) %}

<bolt-layout-item
  {{ source_order ? 'source-order="' ~ source_order ~ '"' : '' }}
  {{ item_attributes }}
>
  {{ content }}
</bolt-layout-item>
