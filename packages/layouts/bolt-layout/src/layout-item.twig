{% set item_attributes = create_attribute(attributes|default({})) %}

<bolt-layout-item
  {{ columns ? 'columns="' ~ columns ~ '"' : '' }}
  {{ item_attributes }}
>
  {{ content }}
</bolt-layout-item>
