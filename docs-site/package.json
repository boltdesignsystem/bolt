{
  "name": "@bolt/website",
  "version": "5.8.2",
  "private": true,
  "description": "Bolt Design System Website",
  "repository": {
    "type": "git",
    "url": "https://github.com/bolt-design-system/bolt"
  },
  "license": "MIT",
  "scripts": {
    "analyze": "bolt start --analyze",
    "analyze:prod": "bolt build --prod --analyze",
    "bolt:build": "bolt build",
    "build:noisy": "bolt build --verbosity 5",
    "build:pl": "bolt prep && yarn run lerna exec --scope @bolt/uikit-workshop --loglevel=silent -- yarn run build",
    "build:prod": "bolt build --prod",
    "build:webpack": "bolt webpack --prod",
    "clean": "cd ../ && yarn run clean",
    "prep": "bolt prep",
    "serve": "bolt serve",
    "setup": "cd ../ && yarn run setup",
    "start": "bolt start",
    "start:prod": "bolt start --prod",
    "test:links": "blc http://localhost:3000 --recursive --ordered --filter-level 2",
    "watch": "bolt watch"
  },
  "jest": {
    "setupFilesAfterEnv": [
      "jest-expect-message"
    ]
  },
  "dependencies": {
    "@bolt/analytics-autotrack": "^5.0.0",
    "@bolt/components-page-footer": "^5.8.0",
    "@bolt/components-page-header": "^5.8.0",
    "@bolt/components-search-filter": "^5.8.0",
    "@bolt/lazy-queue": "^5.0.0",
    "@bolt/micro-journeys": "^5.8.0",
    "@bolt/starter-kit": "^5.8.2",
    "@bolt/theme-switcher": "^5.8.0"
  },
  "devDependencies": {
    "@ckeditor/ckeditor5-build-classic": "^12.1.0",
    "chalk-cli": "^4.1.0",
    "choices.js": "^7.0.0",
    "deepmerge": "^4.2.2",
    "iframe-resizer": "^4.1.1",
    "jest-expect-message": "^1.0.2",
    "list.js": "^1.5.0",
    "resolve": "^1.15.0",
    "yargs": "^15.1.0"
  }
}
