{% import '@bolt/macros.twig' as macros %}
{# ^ allows us to use inline includes that also work in Drupal #}

<bolt-text>This demo sets the <code>imageValign</code> prop to <code>bottom</code> so the image is always flush with the bottom-edge of the Hero container.</bolt-text>

<bolt-banner>
	<p><strong>Note:</strong> this automatically adjusts the content order on smaller (mobile) screens so the Image displays <em>after</em> the main content instead of before.</p>
</bolt-banner>

{% set blockquote %}
  {% include '@bolt-components-blockquote/blockquote.twig' with {
    size: 'xxlarge',
    weight: 'bold',
    border: 'none',
    'author': {
      'name': 'Oliver Zeeb',
      'title': 'Team Lead, Application Development <br>W&W-Gruppe'
    },
    content: '<p>Pega is one of the key technologies that we are using to rebuild our application landscape.</p>'
  } only %}
{% endset %}
{% include '@bolt-components-hero/hero.twig' with {
  content: blockquote,
  background: '/images/placeholders/backgrounds/background-dark.jpg',
  image: '/images/content/heroes/foreground-person.png',
  image_height: 514,
  image_width: 565,
  imageValign: 'bottom',
  theme: 'dark',
} %}
