{% set description %}
    A representation of a single input checkbox.
{% endset %}

{% set notes %}
  <bolt-ol>
    <bolt-li>Add <code>displayType: 'inline-checkbox'</code> in the label include statement.</bolt-li>
    <bolt-li>Make sure to set input <code>type</code> as <code>checkbox</code></bolt-li>
    <bolt-li>Make the input label cklickable by adding a <code>for</code> attribute to the label element and an <code>id</code> (with the same name as <code>for</code> attribute) to the input checkbox element.</bolt-li>
  </bolt-ol>
{% endset %}

{% set demo %}
{# input checkbox #}
  {% set label %}
    {% include '@bolt-components-form/form-label.twig' with {
      title: 'I accept the terms and conditions',
      displayType: 'inline-checkbox',
      attributes: {
        for: 'checkbox-id',
        name: 'checkbox-name'
      },
    } only %}
  {% endset %}
  {% set input %}
    {% include '@bolt-components-form/form-input.twig' with {
      attributes: {
        type: 'checkbox',
        id: 'checkbox-id',
      },
    } only %}
  {% endset %}
  {% include '@bolt-components-form/form-element.twig' with {
    label: label,
    children: input,
    attributes: {
      class: 'u-bolt-margin-bottom-large'
    }
  } only %}
{% endset %}

{% set twig_markup %}
{% verbatim %}
{% set label %}
  {% include '@bolt-components-form/form-label.twig' with {
    title: 'I accept the terms and conditions',
    displayType: 'inline-checkbox',
    attributes: {
      for: 'checkbox-id',
      name: 'checkbox-name'
    },
  } only %}
{% endset %}
{% set input %}
  {% include '@bolt-components-form/form-input.twig' with {
    attributes: {
      type: 'checkbox',
      id: 'checkbox-id',
    },
  } only %}
{% endset %}
{% include '@bolt-components-form/form-element.twig' with {
  label: label,
  children: input,
  attributes: {
    class: 'u-bolt-margin-bottom-large'
  }
} only %}
{% endverbatim %}
{% endset %}

{% set html_markup %}
{% verbatim %}
  ...
{% endverbatim %}
{% endset %}

{% include '@utils/pattern-doc-page.twig' with {
  title: 'Input checkbox',
  description: description,
  notes: notes,
  demo: demo,
  twig_markup: twig_markup,
  html_markup: html_markup,
} only %}

