{% set schema = bolt.data.components['@bolt-components-text'].schema %}

<bolt-text display="block">
  This is block display (paragraph).
</bolt-text>

<bolt-text display="block">
  This is another block display (paragraph), but the text can also be
  <bolt-text display="inline" font-style="italic">
    inline display
  </bolt-text>
  so this paragraph has inline text inside a block of text.
</bolt-text>
