{% set schema = bolt.data.components["@bolt-components-modal"].schema %}

<p>The modal container's coloring theme can be adjusted by the <code>theme</code> prop. The default is set to <code>xlight</code>. It can also be set to <code>none</code> to make the container completely transparent.</p>

{% for theme in schema.properties.theme.enum %}
  <div class="u-bolt-margin-bottom-medium">
    <h3>{{ theme|capitalize }} Theme</h3>
    {% include "@bolt-components-button/button.twig" with {
      text: "Open Modal",
      size: "small",
      attributes: {
        onclick: "this.nextElementSibling.show()",
      }
    } only %}
    {% include "@bolt-components-modal/modal.twig" with {
      content: "<mark>This is a modal set to " ~ theme ~ " theme.</mark>",
      theme: theme,
    } only %}
  </div>
{% endfor %}
