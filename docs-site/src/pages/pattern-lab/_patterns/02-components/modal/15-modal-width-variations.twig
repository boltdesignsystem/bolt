{% set schema = bolt.data.components["@bolt-components-modal"].schema %}

<p>The modal container's width can be adjusted by the width prop. The default is set to regular, which is about 10 columns wide.</p>

{% for width in schema.properties.width.enum %}
  <div class="u-bolt-margin-bottom-medium">
    <h3>{{ width|capitalize }} Width</h3>
    {% include "@bolt-components-button/button.twig" with {
      text: "Open Modal",
      size: "small",
      attributes: {
        onclick: "this.nextElementSibling.show()",
      }
    } only %}
    {% include "@bolt-components-modal/modal.twig" with {
      content: "This is a modal set to " ~ width ~ " width.",
      width: width,
    } only %}
  </div>
{% endfor %}
