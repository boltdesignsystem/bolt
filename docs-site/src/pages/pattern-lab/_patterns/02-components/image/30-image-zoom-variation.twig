<h4>Image Zoom</h4>
<p>When used with the device viewer component, the image component can used to create a "zoomed" state (`magnify` must be set to `true` on the device viewer).</p>

{% include "@bolt-components-device-viewer/device-viewer.twig" with {
  device: "ipad",
  orientation: "portrait",
  color: "silver",
  image: {
    src: "/images/content/screenshots/device-screenshot--tablet-portrait.jpg",
    lazyload: false
  },
  magnify: true
} %}
