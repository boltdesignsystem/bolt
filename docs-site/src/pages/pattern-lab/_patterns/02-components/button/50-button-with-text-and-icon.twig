{% set schema = bolt.data.components["@bolt-components-button"].schema %}

<h3>Icon usage inside buttons</h3>
<p><mark>Note: when passing icons inside a button, you have to define the icon's position, by default it is set to after, which means the icon will come after the text.</mark></p>

<h3>Icon position set to before</h3>
<div class="u-bolt-margin-bottom-medium">
  {% include "@bolt-components-button/button.twig" with {
    text: "This button has text and icon",
    icon: {
      name: "chevron-left",
      position: "before"
    }
  } only %}
</div>

<h3>Icon position set to after</h3>
<div class="u-bolt-margin-bottom-medium">
  {% include "@bolt-components-button/button.twig" with {
    text: "This button has text and icon",
    icon: {
      name: "chevron-right",
      position: "after"
    }
  } only %}
</div>
