{% set schema = get_data('@bolt/button.schema.json') %}

{% for rounded in schema.properties.rounded.enum %}
  <h4>{{ rounded }} Button</h4>

  {% include "@bolt/button.twig" with {
    "rounded": rounded,
    "text": "Example " ~ rounded ~" Button"
  } %}
{% endfor %}